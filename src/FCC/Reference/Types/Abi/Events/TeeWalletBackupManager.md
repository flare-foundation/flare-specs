# TeeWalletBackupManager Events

### BackupRestoreTriggered

Emitted by: `backupRestore()`

```solidity
event BackupRestoreTriggered(
    address indexed teeId,
    bytes32 indexed walletId,
    uint64 indexed keyId,
    uint256 nonce
);
```
